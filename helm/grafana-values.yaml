# helm/grafana-values.yaml
adminPassword: ${admin_password}
persistence:
  enabled: false
service:
  type: NodePort
  nodePort: 30001
datasources:
  loki:
    type: loki
    url: http://loki:3100
    isDefault: true